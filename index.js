// For React 18
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);

// Or if you want to keep using the older syntax that's compatible with both:
// ReactDOM.render(<App />, document.getElementById('root')); 